{% extends 'frontbase.html.twig' %}

{% block content %}
    <h1>Alışveriş Sepetini Düzenle</h1>

    {{ include('shopcart/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('shopcart_index') }}">back to list</a>

    {{ include('shopcart/_delete_form.html.twig') }}
{% endblock %}
