{% extends 'adminbase.html.twig' %}

{% block title %}Kategori Listesi 📃 {% endblock %}

{% block content %}
    <div class="col-sm-9 col-sm-offset-3 col-lg-10 col-lg-offset-2 main">
        <div class="row">
            <ol class="breadcrumb">
                <li><a href="#">
                        <em class="fa fa-home"></em>
                    </a></li>
                <li class="active">Panel</li>
            </ol>
        </div><!--/.row-->

        <section class="content">
            <div class="box">
                <div class="panel panel-default">
                    <h4 class="panel-heading">Kategori Listesi
                        <small class="panel-container"><a href="{{ path('admin_category_new') }}"
                                                          class="btn btn-lg fa fa-plus pull-right btn-info"> Yeni
                                Kategori Ekle</a></small>
                    </h4>
                    <!-- /.box-header -->
                    <div class="panel-body">
                        <table class="table table-bordered">
                            <tbody>
                            <tr>
                                <th>Id</th>
                                <th>Parent Id</th>
                                <th>Başlık</th>
                                <th>Anahtar Kelime</th>
                                <th>Açıklama</th>
                                <th>Durum</th>
                                <th>Göster</th>
                                <th>Düzenle</th>
                            </tr>
                            <tbody>
                            {% for category in categories %}
                                <tr>
                                    <td>{{ category.id }}</td>
                                    <td>{{ category.parentid }}</td>
                                    <td>{{ category.title }}</td>
                                    <td>{{ category.keywords }}</td>
                                    <td>{{ category.description }}</td>
                                    <td>{{ category.status }}</td>
                                    <td>
                                        <a href="{{ path('admin_category_show', {'id': category.id}) }}" class="btn btn-md btn-block btn-primary btn-info">göster</a></td>
                                    <td>
                                        <a href="{{ path('admin_category_edit', {'id': category.id,'pid':category.parentid}) }}" class="btn btn-md btn-block btn-primary btn-success">düzenle</a>
                                    </td>

                                </tr>
                            {% else %}
                                <tr>
                                    <td colspan="7">Kayıt bulunamadı</td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>

                    </div>
                </div>
                <!-- /.box-body -->
                <div class="box-footer clearfix">
                    <ul class="pagination pagination-sm no-margin pull-right">
                        <li><a href="#">«</a></li>
                        <li><a href="#">1</a></li>
                        <li><a href="#">2</a></li>
                        <li><a href="#">3</a></li>
                        <li><a href="#">»</a></li>
                    </ul>
                </div>
            </div>
        </section>


    </div>    <!--/.main-->

{% endblock %}
